<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/star"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/star"
        android:supportsRtl="true"
        android:theme="@style/Theme.APP11_23_24"
        tools:targetApi="31">
        <activity
            android:name=".CinquenaActivity"
            android:exported="false" />
        <activity
            android:name=".QuartaActivity"
            android:exported="false" />
        <activity
            android:name=".TerceraActivity"
            android:exported="false" />
        <activity
            android:name=".SegonaActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>